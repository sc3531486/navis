# 06 — 白板容器与 UI 框架详细设计（Whiteboard Container & UI Architecture）

> 模块编号：06 | 模块归属：Navis 通用宿主底座 / 前端视图层  
> 依赖：Solid.js, Cordis Context, SlotStore  
> 被依赖：所有前端扩展、产品壳扩展

---

## 一、白板宿主定位与设计哲学

Navis 前端是一个**纯白板容器（Whiteboard Container）**：
1. **零硬编码物理分区**：宿主白板外壳（`WhiteboardShell.tsx`）绝不预设侧边栏、对话框、终端抽屉或特定产品区域。
2. **纯粹的插槽投影**：宿主只提供 `root`（主工作区）与 `overlay`（全局悬浮遮罩）两个顶级插槽。
3. **扩展自主声明布局**：具体产品形态（如 Navis Code IDE 布局、银行柜面双屏布局）由产品壳扩展挂载到 `root` 上，并在其内部开辟多级子插槽。

---

## 二、架构分层与组件层次

```text
┌─────────────────────────────────────────────────────────────┐
│              HTML 顶层渲染入口 (src/index.tsx)              │
│       ├── installThemeLifecycle()  主题响应式监听            │
│       └── bootstrap(rootContext)   插件加载与启动生命周期     │
└──────────────────────────────┬──────────────────────────────┘
                               │ 挂载
                               ▼
┌─────────────────────────────────────────────────────────────┐
│           通用白板外壳 (src/app/WhiteboardShell.tsx)         │
│  ├── <DynamicSlot name="root" />    主视图视口               │
│  │   └── 挂载产品壳扩展 (如 navis-code StudioLayout)          │
│  │         ├── <DynamicSlot name="navis-code.sidebar.left" />│
│  │         └── <DynamicSlot name="navis-code.viewport.main"/>│
│  └── <DynamicSlot name="overlay" /> 全局浮层与模态弹窗       │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、响应式动态插槽树（DynamicSlot）

### 3.1 插槽组件（`src/core/slots/DynamicSlot.tsx`）

```tsx
import { Component, createSignal, onCleanup, onMount, Show, For, type JSX } from 'solid-js';
import { slotStore } from './SlotStore';
import { componentRegistry } from '../components/ComponentRegistry';

export interface DynamicSlotProps {
  name: string;
  class?: string;
  fallback?: JSX.Element;
}

export const DynamicSlot: Component<DynamicSlotProps> = (props) => {
  const [tick, setTick] = createSignal(0);

  onMount(() => {
    const unsubSlots = slotStore.subscribe(() => setTick((t) => t + 1));
    const unsubComponents = componentRegistry.subscribe(() => setTick((t) => t + 1));
    onCleanup(() => {
      unsubSlots();
      unsubComponents();
    });
  });

  void tick;
  const items = () => slotStore.getContributions(props.name);

  return (
    <Show when={items().length > 0} fallback={props.fallback}>
      <div class={props.class} data-navis-slot={props.name}>
        <For each={items()}>
          {(item) => <>{item.component() as JSX.Element}</>}
        </For>
      </div>
    </Show>
  );
};
```

### 3.2 插槽注册中心与去重覆盖机制（`src/core/slots/SlotStore.ts`）

`SlotStore` 作为响应式插槽注册中心，严格基于 `(id, pluginId)` 唯一键进行去重与原地覆盖更新：

```typescript
register<T>(slotName: string, contribution: SlotRegistration): () => void {
  const list = this.slots.get(slotName) ?? [];
  const entry: SlotContribution = {
    ...contribution,
    priority: contribution.priority ?? 100,
  };
  // 智能去重：若同一插件已注册过相同 ID，原地更新覆盖，杜绝双份组件
  const existingIndex = list.findIndex(
    (s) => s.id === entry.id && s.pluginId === entry.pluginId,
  );
  if (existingIndex !== -1) {
    list[existingIndex] = entry;
  } else {
    list.push(entry);
  }
  list.sort((a, b) => a.priority - b.priority);
  this.slots.set(slotName, list);
  this.notify();
  return () => {
    const idx = list.findIndex(
      (s) => s.id === entry.id && s.pluginId === entry.pluginId,
    );
    if (idx !== -1) {
      list.splice(idx, 1);
      this.notify();
    }
  };
}
```

### 3.3 插槽通用 CSS 布局约束

通用插槽包装元素（如 `.navis-sidebar-slot` 与 `.navis-main-slot`）统一采用 Flex 弹性流布局，保证内部子扩展在 100% 高度下自适应伸缩且不溢出破坏整体视口。

---

## 四、通用主题系统（Theme System）

宿主提供基于 CSS 变量的设计令牌（Design Tokens）与响应式主题切换引擎（`src/theme/index.ts`）：
- **模式支持**：`light`（浅色）、`dark`（深色）、`system`（跟随操作系统）。
- **DOM 激活**：通过在 `<html>` 元素上设置 `data-theme="light|dark"` 属性实现零成本切换。
- **持久化**：自动持久化至本地 `localStorage`，启动瞬时恢复。
