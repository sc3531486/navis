# Navis Go 架构迁移与重构计划

> 更新日期：2026-08-21
> 状态：万物皆扩展架构重构执行中。

## 一、不可变架构边界

1. **框架定位**：`src/` 与 `src-tauri/src/` 为通用应用白板与扩展运行时底座，命名为 **Navis**。只提供窗口宿主、扩展发现/加载、Cordis 服务容器、事件流、插槽投影、通用命令、沙箱权限、存储、多路复用 IPC 与流式通道。
2. **扩展定位**：AI、Agent、Gateway、MCP、LSP、会话、项目、任务、编辑器、终端、设置、知识库、记忆，以及银行柜面系统、双录系统等垂直业务，**全部属于扩展（Extensions）**。
3. **产品定位**：产品形态由根级 `<product>.json`（如 `navis-code.json`、`teller-system.json`）声明装配的扩展清单决定。新增产品只需新增扩展目录与产品清单，**严禁修改 `src/` 或 `src-tauri/src/` 源码**。
4. **扩展通信**：扩展统一通过 `ctx.views`（插槽投影）、`ctx.events`（事件总线）、`ctx.commands`（命令调用）、`ctx.services`（IoC 服务）及前后端 IPC 通信，禁止跨扩展直接侵入私有 store。

## 二、目录契约

```text
extensions/<extension-id>/
├── extension.json                    # 扩展清单（id, name, version, main, contributes）
├── README.md
├── ExtensionUI/                      # 前端扩展代码、组件、样式与资源
│   ├── src/index.tsx                 # 前端入口（导出 NavisPlugin）
│   └── styles/                       # 专属样式
└── ExtensionBackend/                 # 后端扩展代码
    ├── src/                          # 后端源码
    └── main.mjs                      # 后端进程入口（stdio JSON-RPC）
```

产品清单（如 `navis-code.json`）：
```json
{
  "id": "navis-code",
  "name": "Navis Code",
  "shell": "navis-code",
  "description": "Navis Code AI Agent IDE",
  "extensions": [
    "navis-agent-core",
    "navis-ai-platform",
    "navis-editor",
    "navis-knowledge",
    "navis-memory",
    "navis-project",
    "navis-session",
    "navis-settings",
    "navis-task",
    "navis-terminal",
    "navis-demo"
  ]
}
```

## 三、重构阶段与执行项

### 3.1 框架层纯净化与 Cordis 原语完善
- 前端 `src/core/context.ts`：支持 services DI (`provide`/`use`/`has`)、events (`on`/`emit`/`waterfall`/`serial`/`parallel`)、views (`register`/`items`)、commands (`register`/`execute`)。
- 前端 `src/core/manifest/`：泛型 `ContributionRegistry`，支持任意插件注册扩展贡献点处理器。
- 移出 `src/` 中原有的 Agent 专有逻辑（`AgentPipeline.ts`、`ToolRegistry.ts`）至对应扩展。
- 清理 `src/styles/` 中残留的产品专属 CSS，仅保留通用白板重置、主题与表面样式。
- 后端 `src-tauri/src/kernel/manifest.rs`：泛型 `contributes` 结构，移除硬编码业务。
- 后端使用 `tracing` 替代 `println!`。

### 3.2 Navis Code 业务套件自持有
- `navis-code`：声明根布局 `root` 插槽与子插槽。
- `navis-agent-core`：自持有 Agent 管线、Composer 与时间线。
- `navis-ai-platform`：自持有 ToolRegistry、MCP/LSP 与 API Gateway。
- `navis-session`、`navis-project`、`navis-editor`、`navis-terminal`、`navis-settings`、`navis-knowledge`、`navis-memory`、`navis-task`、`navis-demo`：完整实现专属 UI 入口与自持状态。

## 四、验收标准

- `src/` 与 `src-tauri/src/` 无任何 Agent/Session/Project/Terminal 等特定业务代码或产品 ID 特判。
- 切换 `NAVIS_PRODUCT=teller-system` 时可作为纯净白板/柜面系统启动，无需修改框架代码。
- `npm run build`、`cargo check`、`cargo test` 全量通过。
