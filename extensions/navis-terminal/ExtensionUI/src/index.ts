/**
 * ============================================================
 * navis-terminal 扩展前端 — 迁移过渡期 re-export 桥
 * ============================================================
 *
 * 实际文件仍保留在 src/components/Terminal/
 * 后续 Phase 执行物理搬迁时，此处的 re-export 路径将同步更新。
 *
 * 归属说明：
 *   - Terminal 组件 → 扩展 navis-terminal
 * ============================================================
 */

export { default as TerminalPanel } from '@/components/Terminal/TerminalPanel';
