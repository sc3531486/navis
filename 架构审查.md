# Navis Go 架构审查

> 审查日期：2026-08-21
> 范围：通用 Navis 白板框架、`extensions/` 扩展套件及产品装配形态。
> 原则：以当前目录和入口为事实，确保框架层零业务逻辑侵入。

## 1. 架构结论

- **框架纯净化成立**：底层框架 `navis`（`src/` 与 `src-tauri/src/`）已经收敛为通用桌面白板和扩展运行时，提供 Cordis DI 服务容器、事件流、插槽树、泛型贡献分发、通用 IPC 路由器与安全沙箱。
- **业务全面插件化**：所有 Agent、会话、文件树、编辑器、终端、设置、知识库、记忆、任务等能力全部作为独立扩展存在于 `extensions/<extension-id>/` 目录中。
- **产品形态由配置驱动**：产品（如 `navis-code`、`teller-system`）由根目录 `<product>.json` 声明装配的扩展列表，宿主零改动。

## 2. 目录职责

| 目录 | 职责与归属 |
|---|---|
| `src/` | Navis 宿主前端：通用应用白板、Cordis DI 上下文、插槽树投影、扩展加载与桥接 |
| `src-tauri/src/kernel/` | Navis 宿主后端：扩展清单解析、动态路由注册表、产品装配清单过滤 |
| `src-tauri/src/core/` | Navis 宿主后端：通用多路复用 IPC 路由器、插件进程管理器、安全沙箱 |
| `extensions/navis-code/` | Navis Code 产品壳扩展：声明产品级布局插槽与窗口结构 |
| `extensions/navis-*/` | Navis Code 业务扩展套件（Agent、AI 网关、会话、项目、编辑器、终端、设置等） |
| `extensions/navis-demo/` | 独立扩展示例，验证第三方插件扩展能力 |

## 3. 边界断言规则

运行以下命令审计框架纯净度：
```bash
rg -i "agent|session|composer|terminal|project|task|diff" src/ src-tauri/src/
```
命中应仅限于通用协议注释或泛型桥接，框架层严禁包含具体业务实现或产品 ID 特判。
